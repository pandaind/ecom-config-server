spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pandaind/ecom-configs
          clone-on-start: true
          default-label: main
  kafka:
    bootstrap-servers: http://localhost:9092

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
